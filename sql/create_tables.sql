CREATE TABLE sales (
    order_id INT,
    order_date DATE,
    customer_name VARCHAR(255),
    region VARCHAR(255),
    product VARCHAR(255),
    category VARCHAR(255),
    quantity INT,
    unit_price numeric,
    revenue numeric,
    cost numeric,
    profit numeric

);